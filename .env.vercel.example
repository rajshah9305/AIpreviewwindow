# Vercel Environment Variables
# These are optional - users configure API keys in the Settings UI

# Node Environment (automatically set by Vercel)
NODE_ENV=production

# Optional: Default AI Model Settings (can be overridden in UI)
# DEFAULT_MODEL=gpt-4
# DEFAULT_API_KEY=your-api-key-here
# DEFAULT_BASE_URL=https://api.openai.com/v1
